<div class="flex justify-between py-2 m-6 border rounded-md bg-slate-200">
  <div class="mx-5 my-2">
    <%= link_to board.name, board_path(board.id) %>
  </div>
  <div >
    <%= link_to '刪除', board_path(board.id), method: 'delete', data: {confirm: 'sure?' }, class: 'btn-danger mx-2' %>
  </div>
</div>
    